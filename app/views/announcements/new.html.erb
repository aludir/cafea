<script>
  var tags = [<% @tags.each do |tag| %>"<%= tag.name %>",<% end %>];
  
$(document).ready(function() {
  $(".js-example-basic-multiple").val(['<%= @selected.to_s%>']).select2({
  	tags: tags,
  	tokenSeparators: [',', ' ']
  });
});
</script>

<%= render 'form', dom_id: 'new_announcement'%>
