<h1>Listing users</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to user.surname, user %></td>
        <% if user.id == current_user.id %>
          <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <%end%>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
